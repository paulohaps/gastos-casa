name: Validate Gastos

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Syntax check
        run: |
          node --check js/api.js
          node --check js/app.js
          node --check js/ui.js
          node --check js/core/utils.js
          node --check js/core/behavior-engine.js
          node --check js/core/receipt-parser.js
          node --check js/modules/navigation.js
          node --check js/modules/smart-entry.js
          node --check js/modules/scanner.js
          node --check js/modules/receipt-import.js
          node --check js/modules/radar.js
          node --check js/modules/budgets.js
          node --check js/modules/members.js
          node --check js/modules/recurring.js
          node --check js/modules/expenses.js
          node --check js/modules/insights.js
          node --check js/modules/dashboard.js
          node --check backend/index.mjs
          node --check backend/services/smart-entry-service.mjs
          node --check backend/services/receipt-service.mjs
          node --check backend/routes/smart-entry.mjs
          node --check backend/routes/receipts.mjs
          node --check backend/receipts/nfce-parser.mjs
          node --check backend/routes/expenses.mjs
          node --check backend/routes/budgets.mjs
          node --check backend/routes/members.mjs
          node --check backend/routes/recurring.mjs
          node --check backend/smart-entry/parser.mjs
          node --check backend/smart-entry/ai-provider.mjs
          node --check backend/smart-entry/metrics.mjs
          node --check sw.js

      - name: Smart Entry unit tests
        run: npm run test:unit

      - name: Backend public smoke
        run: |
          curl --fail --retry 2 --max-time 10 https://br-polished-voice-a5flam43-gastospwa.compute.c-1.us-east-2.aws.neon.tech/health | grep -q '"ok":true'
          curl --fail --retry 2 --max-time 10 https://br-polished-voice-a5flam43-gastospwa.compute.c-1.us-east-2.aws.neon.tech/features | grep -q '"smartEntry":true'

      - name: Frontend DOM/API contract
        run: node scripts/check-frontend.mjs

      - name: Backend contract checks
        run: |
          grep -q "async salvarOrcamentos" js/api.js
          grep -q "Array.isArray(body.items)" backend/routes/budgets.mjs
          grep -q "url.pathname !== '/budgets'" backend/routes/budgets.mjs
          grep -q "url.pathname !== '/recurring'" backend/routes/recurring.mjs
          grep -q "path === '/expenses'" backend/routes/expenses.mjs
          grep -q "url.pathname !== '/members'" backend/routes/members.mjs
          grep -q "path === '/smart-entry/parse'" backend/routes/smart-entry.mjs
          grep -q "path === '/smart-entry/rules'" backend/routes/smart-entry.mjs
          grep -q "path === '/smart-entry/metrics'" backend/routes/smart-entry.mjs
          grep -q "path === '/features'" backend/index.mjs
          grep -q "url.pathname === '/receipts/inspect'" backend/routes/receipts.mjs
          grep -q "receipt_imports" db/migrations/20260919_receipt_imports.sql
          grep -q "rules-learning-history-v4" backend/index.mjs
          grep -q "inputMode" backend/routes/smart-entry.mjs
          grep -q "smart_entry_rules" db/migrations/20260919_smart_entry_learning.sql
          grep -q "smart_entry_telemetry" db/migrations/20260919_smart_entry_telemetry.sql

  visual:
    needs: validate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install dependencies
        run: npm install

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Start static server
        run: python3 -m http.server 4173 >/tmp/gastos-http.log 2>&1 &

      - name: Visual smoke
        run: npm run test:visual

      - name: Upload screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: visual-smoke
          path: test-results/**/*.png
          if-no-files-found: ignore
